### known bugs
when previewing scripts in AvsP you'll have to apply AssumeFieldBased()/AssumeTFF()/AssumeBFF() to the first clip.  this is because value of field parity in AvsP is hardcoded.  VirtualDub and any other kind of software should not have this issue.
